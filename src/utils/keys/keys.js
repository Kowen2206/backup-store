const keys = {
    paypalPaymentClientID: String(process.env.REACT_APP_PAYPAL_CLIENT_ID),
    api_url: process.env.REACT_APP_API_URL
    // fireBaseToken: String(process.env.FIREBASE_TOKEN),
  }
  
  export default keys;